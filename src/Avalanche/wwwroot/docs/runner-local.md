# Running Scenarios in Avalanche
  
All test scenarios can be executed directly from the **Client Application**.  
Each test runs in a separate thread, allowing multiple scenarios to be executed concurrently.
  
To start a test, simply select a **Scenario** from the list and click **Start**.